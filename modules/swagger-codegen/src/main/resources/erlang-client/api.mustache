-module({{classname}}).

%% generated methods
{{#operations}}{{#operation}}-export([{{operationId}}/3]).
{{/operation}}{{/operations}}
{{#operations}}{{#operation}}
-spec {{operationId}}(Body::term(), Headers::list(), Params::term()) ->
  {ok, integer(), list(), {ok, binary()}}.
{{operationId}}(Body, Headers, Params) ->
  Url = swagger_utils:get_url("{{basePathWithoutHost}}{{path}}"),
  PreparedBody = jsx:encode(Body),
  FilledUrl = swagger_utils:fill_url(Url, Params),

  hackney:request({{httpMethod}}, Url, Headers, PreparedBody, [with_body]).
{{/operation}}{{/operations}}
